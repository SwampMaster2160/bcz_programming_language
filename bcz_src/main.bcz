B = 5;

main = @entry_point () {
	b = GetConsoleWindow();
	//FillConsoleOutputCharacterA(b, 'a', 10, 3, 2, 0);
	Beep(500, 1000);
	a = 2 + B;
	420 + aFn(a, 2)
};

aFn = (a, b) {
	a + b
};

Beep = @link (4, 4) -4;
GetConsoleWindow = @link () 8;
FillConsoleOutputCharacterA = @link (8, 1, 4, 2, 2, 8) -4;